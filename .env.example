# Server-only mail settings. Never prefix these with VITE_.
MAIL_FROM=info@hayth-ai.com
MAIL_TO=info@hayth-ai.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Use json to test without SMTP. Remove this in production when SMTP is set.
MAIL_TRANSPORT=json
